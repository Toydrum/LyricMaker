// Environment file for production builds.
// This file replaces `environment.ts` when you build with `--configuration production`.

export const environment = {
  production: true,
  apiBaseUrl: 'https://api/syllables/split-syllables'
};
